# create-dmg 1.3.0

Vendored from [create-dmg/create-dmg](https://github.com/create-dmg/create-dmg)
at `a2b71d0` (MIT). `@quickgui/cli` runs this script to package production macOS
disk images. See `LICENSE`.
