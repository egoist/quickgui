[package]
name = "quickgui-extension-{{NAME}}"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "{{LIBRARY}}"
crate-type = ["cdylib"]

[profile.release]
panic = "abort"
strip = "symbols"

# This extension is independent of any parent Cargo workspace.
[workspace]
