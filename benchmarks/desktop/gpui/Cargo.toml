[package]
name = "benchmark-gpui"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
gpui = "=0.2.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Keep this fixture independent of the repository workspace.
[workspace]
