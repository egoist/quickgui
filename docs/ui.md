# QuickGUI UI

The application language is Go. Start with the [Go guide](go.md) for `ui.View`, flat style props, signals, and `Component: Counter`.

The Rust core continues to own native behavior. The [Go SDK](../go/README.md) exposes its retained primitives, compound controls, routing, and SwiftUI hosting. The TypeScript CLI remains the development and packaging tool.
