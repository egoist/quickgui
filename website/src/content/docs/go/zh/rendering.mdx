组件在挂载时运行，并返回应显示的树。之后的信号更新会改变已绑定的文本和属性。当子节点结构本身会变化时，使用 `ui.Show` 和 `ui.For`。

## 渲染模型

每个组件都返回 `*ui.Element`。将元素作为子节点传入，或多个同级节点返回 `ui.Fragment(...)`。始终使用你创建的元素：返回它、赋值，或传给父节点。

```go
func ViewExample() *ui.Element {
	return ui.View().
		Padding(20).
		Gap(12).
		Child(ui.Text("Hello from Go")).
		Child(ui.Button("Continue").OnClick(func() {}))
}
```

## 条件内容

普通的 Go `if` 只在组件创建时运行。子节点需要稍后出现或消失时，使用 `ui.Show`。

```go
func DisclosureExample() *ui.Element {
	open, setOpen := ui.CreateSignal(false)
	return ui.View().Children(
		ui.Button("Toggle details").OnClick(func() { setOpen(!open()) }),
		ui.Show(
			open,
			func() *ui.Element { return ui.Text("Details") },
		),
	)
}
```

条件保持在同一分支时，该分支会保留状态。离开该分支时，其副作用和订阅会被释放。

## 列表与节点身份

对可能变化的列表使用 `ui.For`。为每一行提供稳定且唯一的键，例如数据库 ID，而不是位置。`ui.KeyedFor` 还会给行一个当前条目的访问器，因此重命名会更新标签，而不会重建该行。

编辑列表数据时，向设置函数传入新切片。原地修改已存储的切片不会通知 UI。

```go
type ListPerson struct {
	ID   int
	Name string
}

func KeyedListExample() *ui.Element {
	people, setPeople := ui.CreateSignal([]ListPerson{{1, "Ada"}, {2, "Grace"}})
	return ui.View().Children(
		ui.Button("Reorder and rename").OnClick(func() {
			setPeople([]ListPerson{{2, "Grace"}, {1, "Ada Lovelace"}})
		}),
		ui.KeyedFor(
			people,
			func(person ListPerson) any { return person.ID },
			func(person func() ListPerson, index func() int) *ui.Element {
				return ui.Text(index(), ": ", person().Name)
			},
			func() *ui.Element { return ui.Text("No people") },
		),
	)
}
```

重排会移动现有行。对于大型集合，使用虚拟化表格或列表，只挂载可见行。

## 窗口生命周期

每个窗口都有自己的组件根。关闭窗口会释放其元素、副作用和 `ui.OnCleanup` 回调。

在组件挂载时启动后台工作。`ui.Async` 在 UI goroutine 之外运行 `load`，再把结果写回；关闭窗口或移除该分支会取消上下文，并忽略迟到的结果。

```go
func LoadingPanel(load func(context.Context) (string, error)) *ui.Element {
	result, setResult := ui.CreateSignal("Loading…")
	ui.Async(
		load,
		func(value string, err error) {
			if err != nil {
				setResult(err.Error())
				return
			}
			setResult(value)
		},
	)
	return ui.Text(result())
}
```

## 当前窗口

在组件挂载时捕获 `native.CurrentWindow()`，然后在回调中使用该句柄。

```go
func CloseButton() *ui.Element {
	window := native.CurrentWindow()
	return ui.Button("Close window").OnClick(func() { window.Close() })
}
```

## 检查、测试与调试

在应用模块中执行：

```bash
bun run test
bun run fmt --check
```

`test` 编译应用使用的同一套视图。标准 Go 诊断和调试器可直接用于原始源文件。
