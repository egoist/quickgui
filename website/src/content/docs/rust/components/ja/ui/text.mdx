継承されたタイポグラフィ、選択、アクセシビリティを使って Unicode テキストを組版・描画します。

## インポート

`quickgui` から `text` をインポートします。

```rust
use quickgui::*;
```

## 使い方

```rust
text("Hello from Rust").text_lg()
```

## 主なプロパティ

| プロパティ | 説明 |
| --- | --- |
| `child` | コンポーネント内にマウントする内容です。 |
| `ref` | 保持されたネイティブノードを受け取ります。 |
