使用继承的排版、文本选择和无障碍属性来排版并绘制 Unicode 文本。

## 导入

从 `quickgui` 导入 `text`。

```rust
use quickgui::*;
```

## 用法

```rust
text("Hello from Rust").text_lg()
```

## 主要属性

| 属性 | 说明 |
| --- | --- |
| `child` | 挂载在组件内部的内容。 |
| `ref` | 接收保留式原生节点。 |
