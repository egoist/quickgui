[package]
name = "quickgui-downstream-smoke"
version = "0.0.0"
edition = "2024"
rust-version = "1.90"
publish = false

[dependencies]
quickgui = { version = "=0.1.4-next.3", features = ["inspector"] }
